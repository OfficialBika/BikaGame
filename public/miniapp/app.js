(() => {
  const CORE = '/miniapp/app-core.js?v=20260915';
  const tg = window.Telegram?.WebApp;
  const initData = tg?.initData || '';
  const api = async (path, body = {}) => {
    const res = await fetch(path, { method:'POST', headers:{'Content-Type':'application/json','X-Telegram-Init-Data':initData}, body:JSON.stringify(body) });
    const data = await res.json().catch(() => ({}));
    if (!res.ok || data.ok === false) throw new Error(data.message || data.error || 'Request failed');
    return data;
  };
  function esc(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;')}
  function profile(){return tg?.initDataUnsafe?.user||{}}
  function applyProfile(){const u=profile(),a=document.getElementById('avatarText'),n=document.getElementById('playerNameText'),name=[u.first_name,u.last_name].filter(Boolean).join(' ')||u.username||'Bika Player';if(n)n.textContent=name;if(a){if(u.photo_url){a.textContent='';a.style.backgroundImage=`url("${u.photo_url.replaceAll('"','%22')}")`}else a.textContent=name.slice(0,2).toUpperCase()}}
  function ensureModal(){let m=document.getElementById('bikaNavModal');if(m)return m;m=document.createElement('div');m.id='bikaNavModal';m.className='bika-nav-modal';m.innerHTML='<div class="bika-modal-backdrop"></div><section class="bika-modal-sheet" role="dialog" aria-modal="true"><button class="bika-modal-close" type="button">×</button><div id="bikaModalContent"></div></section>';document.body.appendChild(m);m.querySelector('.bika-modal-backdrop').addEventListener('click',closeModal);m.querySelector('.bika-modal-close').addEventListener('click',closeModal);return m}
  function closeModal(){document.getElementById('bikaNavModal')?.classList.remove('open')}
  function showModal(html){const m=ensureModal(),c=document.getElementById('bikaModalContent');if(c)c.innerHTML=html;m.classList.add('open')}
  async function showHistory(){showModal('<div class="bika-modal-eyebrow">BIKA ACTIVITY</div><h2>Game History</h2><p class="bika-modal-muted">Recent bets and results</p><div id="bikaHistoryRows" class="bika-history-rows"><div class="bika-loading">Loading history…</div></div>');try{const d=await api('/api/mini/history',{game:'all',limit:40}),rows=document.getElementById('bikaHistoryRows'),items=d.items||[];if(!rows)return;rows.innerHTML=items.length?items.map(x=>{const net=Number(x.net||0),label=x.label||(x.multiplier?`x${Number(x.multiplier).toFixed(2)}`:x.outcome||'Game'),time=x.createdAtMs?new Date(x.createdAtMs).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}):'—';return `<div class="bika-history-row"><div><b>${esc(label)}</b><span>${esc(x.title||x.game||'Game')} · ${time}</span></div><strong class="${net>=0?'pos':'neg'}">${net>=0?'+':''}${net.toLocaleString('en-US')} ${esc(document.getElementById('stickyBalanceText')?.textContent?.replace(/[0-9,.-]/g,'').trim()||'')}</strong></div>`}).join(''):'<div class="bika-empty">No history yet.</div>'}catch(e){const r=document.getElementById('bikaHistoryRows');if(r)r.innerHTML=`<div class="bika-empty">${esc(e.message||'Unable to load history')}</div>`}}
  function showWallet(){const u=profile(),balance=document.getElementById('balanceText')?.textContent||'—',name=[u.first_name,u.last_name].filter(Boolean).join(' ')||u.username||'Bika Player';showModal(`<div class="bika-modal-eyebrow">BIKA WALLET</div><h2>Your Balance</h2><div class="bika-wallet-big">${esc(balance)}</div><div class="bika-wallet-user"><span>Account</span><b>${esc(name)}</b></div>`)}
  function showProfile(){const u=profile(),name=[u.first_name,u.last_name].filter(Boolean).join(' ')||u.username||'Bika Player',photo=u.photo_url||'',avatar=photo?`<img src="${esc(photo)}" alt="Profile">`:`<span>${esc(name.slice(0,2).toUpperCase())}</span>`;showModal(`<div class="bika-profile-head"><div class="bika-profile-avatar">${avatar}</div><div><div class="bika-modal-eyebrow">TELEGRAM PLAYER</div><h2>${esc(name)}</h2><p>${u.username?'@'+esc(u.username):'Telegram account'}</p></div></div><div class="bika-profile-grid"><div><span>User ID</span><b>${esc(u.id||'—')}</b></div><div><span>Language</span><b>${esc(u.language_code||'—')}</b></div><div><span>Premium</span><b>${u.is_premium?'Yes':'No'}</b></div><div><span>Balance</span><b>${esc(document.getElementById('balanceText')?.textContent||'—')}</b></div></div>`)}
  function setupNavigation(){const nav=document.querySelector('.nav-tabs');if(!nav)return;nav.innerHTML='<button class="tab active" type="button" data-bika-nav="home">Home</button><button class="tab" type="button" data-bika-nav="games">Games</button><button class="tab" type="button" data-bika-nav="history">History</button><button class="tab" type="button" data-bika-nav="wallet">Wallet</button><button class="tab" type="button" data-bika-nav="profile">Profile</button>';nav.querySelectorAll('[data-bika-nav]').forEach(b=>b.addEventListener('click',()=>{nav.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x===b));const t=b.dataset.bikaNav;if(t==='home')window.openPanel?.('home');if(t==='games'){window.openPanel?.('home');setTimeout(()=>document.querySelector('.game-grid')?.scrollIntoView({behavior:'smooth',block:'start'}),50)}if(t==='history')showHistory();if(t==='wallet')showWallet();if(t==='profile')showProfile()}))}
  function addRocketHistoryButton(){const card=document.querySelector('#rocket .small-history');if(!card||document.getElementById('rocketHistoryOpen'))return;const b=document.createElement('button');b.id='rocketHistoryOpen';b.className='rocket-history-open';b.type='button';b.textContent='View History';b.addEventListener('click',showHistory);card.parentElement?.insertBefore(b,card)}
  function boot(){applyProfile();setupNavigation();addRocketHistoryButton();try{tg?.setHeaderColor?.('#0b1c27');tg?.setBackgroundColor?.('#061018');tg?.setBottomBarColor?.('#07131b')}catch(_){} }
  const s=document.createElement('script');s.src=CORE;s.onload=()=>{boot();setTimeout(boot,250)};s.onerror=()=>console.error('Bika Game core failed to load');document.head.appendChild(s);
})();