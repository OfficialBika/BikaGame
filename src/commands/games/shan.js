const { COIN, SHAN } = require('../../config/constants'); const game = require('../../services/gameService'); const { getUser } = require('../../services/economyService'); const { replyHTML } = require('../../utils/telegram'); const { fmt } = require('../../utils/format'); const { isGroupChat, mentionHtml } = require('../../utils/helpers');
module.exports = (bot)=> bot.hears(/^\.(shan)\s+(\d+)\s*$/i, async(ctx)=>{ if(!isGroupChat(ctx)) return replyHTML(ctx,'ℹ️ group ထဲမှာပဲ သုံးနိုင်ပါတယ်။'); const bet=Number(ctx.match[2]); const target=ctx.message?.reply_to_message?.from; if(bet<SHAN.minBet||bet>SHAN.maxBet) return replyHTML(ctx,`🃏 Usage: Reply + <code>.shan 500</code> Min ${fmt(SHAN.minBet)} Max ${fmt(SHAN.maxBet)}`); if(!target?.id||target.is_bot||target.id===ctx.from.id) return replyHTML(ctx,'⚠️ Reply ထောက်ထားတဲ့ user ကိုသာ challenge လုပ်ပါ။'); if(!game.canOpenShan()) return replyHTML(ctx,'⛔ Shan challenge များနေပါတယ်။'); const u=await getUser(ctx.from.id); if(Number(u?.balance||0)<bet) return replyHTML(ctx,'❌ Balance မလုံလောက်ပါ။'); const sent=await replyHTML(ctx,`🃏 <b>Shan Koe Mee Challenge</b>\n━━━━━━━━━━━━\nစိန်ခေါ်သူ: ${mentionHtml(ctx.from)}\nလက်ခံသူ: ${mentionHtml(target)}\nBet: <b>${fmt(bet)}</b> ${COIN}`,{reply_markup:{inline_keyboard:[[ {text:'✅ Accept Shan Duel',callback_data:'SHAN:TEMP'} ],[ {text:'❌ Cancel',callback_data:'SHAN:TEMP_CANCEL'} ]]}}); const id=game.makeId(ctx.chat.id,sent.message_id); await ctx.telegram.editMessageReplyMarkup(ctx.chat.id,sent.message_id,undefined,{inline_keyboard:[[ {text:'✅ Accept Shan Duel',callback_data:`SHAN:ACCEPT:${id}`} ],[ {text:'❌ Cancel',callback_data:`SHAN:CANCEL:${id}`} ]]}); game.setShan(id,{id,chatId:ctx.chat.id,msgId:sent.message_id,bet,challengerId:ctx.from.id,targetUserId:target.id,challenger:ctx.from,target,status:'OPEN',timeoutHandle:setTimeout(()=>game.delShan(id),SHAN.timeoutMs)}); });
