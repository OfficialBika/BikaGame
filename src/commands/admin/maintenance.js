const { ensureTreasury, isOwner, setMaintenance } = require('../../services/treasuryService'); const { replyHTML } = require('../../utils/telegram');
module.exports = (bot)=>{ bot.command('off',async(ctx)=>{ const t=await ensureTreasury(); if(!isOwner(ctx,t)) return replyHTML(ctx,'⛔ Owner only.'); await setMaintenance(true); return replyHTML(ctx,'🛠️ Bot Maintenance Mode <b>ON</b>'); }); bot.command('on',async(ctx)=>{ const t=await ensureTreasury(); if(!isOwner(ctx,t)) return replyHTML(ctx,'⛔ Owner only.'); await setMaintenance(false); return replyHTML(ctx,'✅ Bot Online <b>ON</b>'); }); };
