const { COIN } = require('../../config/constants'); const { ensureTreasury, getTreasury, isOwner, setTotalSupply, setVipWinRate } = require('../../services/treasuryService'); const { parseAmount } = require('../../utils/helpers'); const { replyHTML } = require('../../utils/telegram'); const { fmt } = require('../../utils/format');
module.exports = (bot)=>{ bot.command('settotal',async(ctx)=>{ const t=await ensureTreasury(); if(!isOwner(ctx,t)) return replyHTML(ctx,'⛔ Owner only command.'); const amount=parseAmount(ctx.message.text); if(!amount||amount<=0) return replyHTML(ctx,'Usage: <code>/settotal 5000000</code>'); await setTotalSupply(amount); const tr=await getTreasury(); return replyHTML(ctx,`🏦 <b>Treasury Initialized</b>\n━━━━━━━━━━━━━━\n• Total Supply: <b>${fmt(tr.totalSupply)}</b> ${COIN}\n• Owner Balance: <b>${fmt(tr.ownerBalance)}</b> ${COIN}`); }); bot.command('treasury',async(ctx)=>{ const t=await ensureTreasury(); if(!isOwner(ctx,t)) return replyHTML(ctx,'⛔ Owner only.'); const tr=await getTreasury(); return replyHTML(ctx,`🏦 <b>Treasury Dashboard</b>\n━━━━━━━━━━━━━━\n• Total Supply: <b>${fmt(tr.totalSupply)}</b> ${COIN}\n• Owner Balance: <b>${fmt(tr.ownerBalance)}</b> ${COIN}\n• Owner ID: <code>${tr.ownerUserId}</code>\n• VIP WR: <b>${tr.vipWinRate}%</b>`); }); bot.command('setvipwr',async(ctx)=>{ const t=await ensureTreasury(); if(!isOwner(ctx,t)) return replyHTML(ctx,'⛔ Owner only.'); const rate=String(ctx.message.text).split(/\s+/)[1]; if(rate==null) return replyHTML(ctx,'Usage: <code>/setvipwr 90</code>'); const n=await setVipWinRate(rate); return replyHTML(ctx,`✅ VIP Win Rate Updated: <b>${n}%</b>`); }); bot.command('vipwr',async(ctx)=>{ const t=await ensureTreasury(); if(!isOwner(ctx,t)) return replyHTML(ctx,'⛔ Owner only.'); return replyHTML(ctx,`📊 VIP Win Rate: <b>${t.vipWinRate}%</b>`); }); };
